<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}TicketFlow{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/index.css') }}">
        {% endblock %}
        <script src="https://kit.fontawesome.com/d079352595.js" crossorigin="anonymous"></script>
    </head>

    <body>
        <header>
            {% block header %}
                <nav class="navbar">
                    <div id="logo">
                        <i class="fa fa-ticket" aria-hidden="true"></i>
                        <h2>TicketFlow</h2>
                    </div>
                    <ul id="navigation">
                        <li><a href="{{ path('home') }}" {% if current_page is defined and current_page == 'home' %}class="active"{% endif %}>Home</a></li>
                        <li><a href="{{ path('login') }}" {% if current_page is defined and current_page == 'login' %}class="active"{% endif %}>Login</a></li>
                        <li><a href="{{ path('signup') }}" {% if current_page is defined and current_page == 'signup' %}class="active"{% endif %}>Sign up</a></li>
                    </ul>
                </nav>
            {% endblock %}
        </header>

        <main class="{% block main_class %}{% endblock %}">
            {% block content %}{% endblock %}
        </main>

        <footer>
            {% block footer %}
                <p>© {{ "now"|date("Y") }} TicketFlow. All rights reserved.</p>
            {% endblock %}
        </footer>

        {% block javascripts %}{% endblock %}
    </body>
</html>